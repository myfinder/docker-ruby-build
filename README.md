# ruby-build docker image

This docker image installs make, gcc and [ruby-build]() to /usr/bin/ruby-build on ubuntu

## Usage

```
$ docker run -t -i myfinder/ruby-build bash -l
root@....:/# --version
```
